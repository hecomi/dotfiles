filename=array.c
macro=false
private=false
type=VALUE
name=rb_ary_new2
params=(long len)


長さ len 分だけメモリを確保した、
長さゼロの Ruby の配列を作成し返します。

対応するRubyコード

  ary = Array.new(len)

使用例

  VALUE ary;
  long len;
  ...
  ary = rb_ary_new2(len);

