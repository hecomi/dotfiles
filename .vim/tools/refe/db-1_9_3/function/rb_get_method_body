filename=eval.c
macro=false
private=true
type=NODE *
name=rb_get_method_body
params=(VALUE *klassp, ID *idp, int *noexp)


クラス klass から id という名前のメソッドエントリを検索する。
見付かったらその本体である構文木を返す。見付からなければ
NULL を返す。

検索結果をキャッシュする。

