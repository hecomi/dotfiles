names=add_introspection
visibility=public
kind=defined

--- add_introspection
Adds the introspection handlers "system.listMethods", "system.methodSignature" and "system.methodHelp",
where only the first one works.

