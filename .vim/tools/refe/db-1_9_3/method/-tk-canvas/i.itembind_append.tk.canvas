names=itembind_append
visibility=public
kind=defined

--- itembind_append(tag, context, *args)
@todo

def itembind_append(tag, context, cmd = Proc.new, *args)

  _bind_append([path, "bind", tagid(tag)], context, cmd, *args)
  self

end

