names=from_file_by_path
visibility=public
kind=defined

--- from_file_by_path(file_path, security_policy = nil) -> Gem::Format
@todo security_policy

Gem ファイルのパスからデータを読み込んで、自身を初期化して返します。

@param file_path Gem ファイルへのパスを指定します。

@param security_policy ???

