type=class
superclass=Resolv=DNS=Query
included=
extended=
library=resolv
aliases=
aliasof=

DNS のすべてのクラスに対するクエリーを表わす
抽象クラスです。

[[m:Resolv::DNS#getresource]] では
[[c:Resolv::DNS::Resource::IN::ANY]] を使うべきです。
