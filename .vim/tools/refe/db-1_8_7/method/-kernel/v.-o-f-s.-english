names=OFS,OUTPUT_FIELD_SEPARATOR
visibility=public
kind=added

--- $OFS                    -> String | nil
--- $OUTPUT_FIELD_SEPARATOR -> String | nil

[[m:$,]] の別名

  require "English"

  array = %w|hoge fuga ugo bar foo|
  p array.join #=> "hogefugaugobarfoo"
  $OUTPUT_FIELD_SEPARATOR = ","
  p array.join #=> "hoge,fuga,ugo,bar,foo"

