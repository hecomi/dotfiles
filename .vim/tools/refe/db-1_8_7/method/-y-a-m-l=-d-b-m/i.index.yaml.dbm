names=index
visibility=public
kind=defined

--- index(value) -> String | nil

value を持つ要素のキーを返します。

対応するキーが見つからなかった場合は nil を返します。

@param value 検索したい値を指定します。YAML 形式に変換できるオブジェク
             トが指定できます。


