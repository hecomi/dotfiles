names=cd
visibility=public
kind=defined

--- cd(path = nil) -> self

pathをカレントディレクトリとするShellオブジェクトを生成します.

@param path カレントディレクトリとするディレクトリを文字列で指定します。


使用例
  require 'shell'
  sh = Shell.new
  sh.cd("/tmp")


