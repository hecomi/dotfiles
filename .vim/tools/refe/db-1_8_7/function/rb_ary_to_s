filename=array.c
macro=false
private=false
type=VALUE
name=rb_ary_to_s
params=(VALUE ary)


ary.to_s

使用例

  void debug_print(VALUE ary)
  {
    Check_Type(ary, T_ARRAY);
    printf("%s", STR2CSTR(rb_ary_to_s(ary)));
  }

